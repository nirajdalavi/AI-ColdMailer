# Copy to .env and fill in before building for Chrome Web Store.
# End users never see or edit this — only you (the publisher) do.

# Gmail OAuth client ID (Chrome Extension type in Google Cloud Console)
VITE_GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
