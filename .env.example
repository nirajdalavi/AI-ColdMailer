# Not committed to git — create your own file:
#   cp .env.example .env
#
# Gmail OAuth client ID from Google Cloud Console
# (Credentials → OAuth client ID → type: Chrome Extension)
VITE_GMAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
